# Set these only on the server or in an ignored local .env file.
DEEPSEEK_API_KEY=
# DS_API_KEY is also supported, including your existing computer environment.
DEEPSEEK_MODEL=deepseek-flash
# Required for authenticated /api/cards requests from external agents.
EVIDEX_AGENT_API_TOKEN=
PORT=3000
